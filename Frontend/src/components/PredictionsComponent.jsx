// PredictionsComponent.jsx
import { Box } from '@chakra-ui/react';

const PredictionsComponent = ({ predictions }) => {
  return (
    <Box>
      {"It's Crazing using CNN model " }
    </Box>
  );
};

export default PredictionsComponent;
